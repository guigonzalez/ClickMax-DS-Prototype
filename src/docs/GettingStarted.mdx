import { Meta } from '@storybook/blocks';
import * as React from 'react';
import { Button } from '../components/primitives/Button';
import { Input } from '../components/primitives/Input';
import { Badge } from '../components/primitives/Badge';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '../components/layout/Card';
import { Plus, Download, Mail, Lock, Check } from 'lucide-react';

<Meta title="Documentation/Getting Started" />

# Começando com ClickMax Design System

Bem-vindo ao ClickMax Design System R! Este guia irá ajudá-lo a começar rapidamente.

---

## 📦 Instalação

```bash
npm install @clickmax/ds
```

---

## ⚙️ Configuração Básica

### 1. Importe os estilos globais

No seu arquivo principal (geralmente `main.tsx` ou `App.tsx`):

```tsx
import '@clickmax/ds/styles/globals.css';
```

### 2. Use os componentes

```tsx
import { Button, Input, Card, CardHeader, CardTitle, CardContent } from '@clickmax/ds';

function App() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Meu App</CardTitle>
      </CardHeader>
      <CardContent>
        <Input label="Nome" placeholder="Digite seu nome" />
        <Button variant="primary">Enviar</Button>
      </CardContent>
    </Card>
  );
}
```

---

## 🎨 Componentes Principais

### Button

O componente Button oferece múltiplas variantes para diferentes contextos:

```tsx
<Button variant="primary">Ação Principal</Button>
<Button variant="secondary">Ação Secundária</Button>
<Button variant="ghost">Ação Terciária</Button>
<Button variant="destructive">Ação Destrutiva</Button>
```

**Exemplo interativo:**

<div style={{ padding: '20px', backgroundColor: '#f5f5f5', borderRadius: '8px', marginTop: '16px' }}>
  <div style={{ display: 'flex', flexWrap: 'wrap', gap: '16px' }}>
    <Button variant="primary">
      <Plus className="h-5 w-5" />
      Primary
    </Button>
    <Button variant="secondary">Secondary</Button>
    <Button variant="ghost">Ghost</Button>
    <Button variant="destructive">Destructive</Button>
    <Button variant="outline">Outline</Button>
  </div>
</div>

---

### Input

Campo de entrada com suporte a label, descrição e erro:

```tsx
<Input
  label="Email"
  type="email"
  placeholder="seu@email.com"
  description="Nunca compartilharemos seu email."
/>

<Input
  label="Senha"
  type="password"
  error="Senha deve ter no mínimo 8 caracteres"
/>
```

---

### Badge

Indicadores visuais compactos para status e categorização:

```tsx
<Badge variant="success">Concluído</Badge>
<Badge variant="warning">Pendente</Badge>
<Badge variant="error">Cancelado</Badge>
```

**Exemplo interativo:**

<div style={{ padding: '20px', backgroundColor: '#f5f5f5', borderRadius: '8px', marginTop: '16px' }}>
  <div style={{ display: 'flex', flexWrap: 'wrap', gap: '16px' }}>
    <Badge variant="default">Default</Badge>
    <Badge variant="success">
      <Check className="h-3 w-3" />
      Success
    </Badge>
    <Badge variant="warning">Warning</Badge>
    <Badge variant="error">Error</Badge>
    <Badge variant="outline">Outline</Badge>
  </div>
</div>

---

### Card

Container versátil para agrupar conteúdo relacionado:

```tsx
<Card>
  <CardHeader>
    <CardTitle>Título do Card</CardTitle>
    <CardDescription>Descrição aqui.</CardDescription>
  </CardHeader>
  <CardContent>
    <p>Conteúdo do card.</p>
  </CardContent>
  <CardFooter>
    <Button>Ação</Button>
  </CardFooter>
</Card>
```

**Exemplo interativo:**

<div style={{ padding: '20px', backgroundColor: '#f5f5f5', borderRadius: '8px', marginTop: '16px', display: 'flex', justifyContent: 'center' }}>
  <Card className="w-[400px]">
    <CardHeader>
      <CardTitle>Card de Exemplo</CardTitle>
      <CardDescription>Este é um exemplo de card do Design System.</CardDescription>
    </CardHeader>
    <CardContent>
      <div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
        <Input label="Nome" placeholder="Digite seu nome" />
        <Input label="Email" type="email" placeholder="seu@email.com" />
      </div>
    </CardContent>
    <CardFooter>
      <Button variant="primary" className="w-full">Enviar</Button>
    </CardFooter>
  </Card>
</div>

---

## 🎯 Ícones (Lucide React)

O Design System inclui a biblioteca Lucide React com 1000+ ícones:

```tsx
import { Settings, User, Download } from 'lucide-react';

<Button variant="primary">
  <Download className="h-5 w-5" />
  Download
</Button>
```

**Exemplo interativo:**

<div style={{ padding: '20px', backgroundColor: '#f5f5f5', borderRadius: '8px', marginTop: '16px' }}>
  <div style={{ display: 'flex', flexWrap: 'wrap', gap: '16px' }}>
    <Button variant="primary">
      <Plus className="h-5 w-5" />
      Criar
    </Button>
    <Button variant="secondary">
      <Download className="h-5 w-5" />
      Download
    </Button>
  </div>
</div>

---

## 🎨 Design Tokens

O Design System usa um sistema de tokens em 3 camadas:

### Cores Semânticas

```tsx
// Classes de fundo
bg-bg-primary           // Fundo principal (#FFFFFF)
bg-bg-secondary         // Fundo secundário (#E8E9EB)
bg-interactive-default  // Fundo interativo (#E4FF00)

// Classes de texto
text-text-primary       // Texto principal (#2A2D31)
text-text-secondary     // Texto secundário (#8B8F96)
text-feedback-success   // Texto de sucesso (#10B981)
text-feedback-error     // Texto de erro (#EF4444)
```

### Espaçamento

Use a escala de espaçamento Tailwind padrão:

```tsx
p-4    // Padding 16px
m-6    // Margin 24px
gap-2  // Gap 8px
```

### Tipografia

```tsx
text-sm font-normal     // Small body text
text-base font-normal   // Default body text
text-lg font-semibold   // Heading 3
text-2xl font-semibold  // Heading 2
text-3xl font-bold      // Heading 1
```

---

## ♿ Acessibilidade

Todos os componentes seguem as melhores práticas de acessibilidade:

- ✅ **Navegação por teclado** - Todos os componentes interativos são acessíveis via teclado
- ✅ **ARIA attributes** - Uso correto de roles, states e properties
- ✅ **Contraste adequado** - Cores seguem WCAG AA guidelines
- ✅ **Screen reader friendly** - Labels e descrições apropriadas

---

## 📚 Próximos Passos

### Explore os componentes

- **[Primitives/Button](/?path=/docs/primitives-button--docs)** - Botões com múltiplas variantes
- **[Primitives/Input](/?path=/docs/primitives-input--docs)** - Campos de entrada
- **[Primitives/Badge](/?path=/docs/primitives-badge--docs)** - Indicadores de status
- **[Layout/Card](/?path=/docs/layout-card--docs)** - Containers para conteúdo

### Veja exemplos práticos

- **[Playground/Interactive](/?path=/docs/playground-interactive--docs)** - Exemplos interativos e composições

### Confira os tokens

- **[Tokens/Colors](/?path=/docs/tokens-colors--docs)** - Paleta de cores completa
- **[Tokens/Typography](/?path=/docs/tokens-typography--docs)** - Escala tipográfica
- **[Tokens/Icons](/?path=/docs/tokens-icons--docs)** - Biblioteca de ícones

---

## 🔗 Recursos Úteis

- 📖 [Documentação completa](https://github.com/clickmax/design-system)
- 🎨 [Lucide Icons](https://lucide.dev/icons) - Catálogo completo de ícones
- 🎭 [Tailwind CSS](https://tailwindcss.com/docs) - Documentação do Tailwind CSS
- ♿ [WCAG Guidelines](https://www.w3.org/WAI/WCAG21/quickref/) - Diretrizes de acessibilidade

---

## 💡 Dicas

### Uso de classes Tailwind

Todos os componentes aceitam `className` para customização:

```tsx
<Button className="w-full">Botão full width</Button>
<Badge className="inline-flex items-center gap-1">Badge com ícone</Badge>
```

### Composição de componentes

Combine componentes para criar interfaces complexas:

```tsx
<Card>
  <CardHeader>
    <div className="flex items-center gap-2">
      <Bell className="h-5 w-5" />
      <CardTitle>Notificações</CardTitle>
    </div>
  </CardHeader>
  <CardContent>
    <Badge variant="error">3 não lidas</Badge>
  </CardContent>
</Card>
```

### Estados interativos

Use estados React para criar experiências dinâmicas:

```tsx
function MyForm() {
  const [loading, setLoading] = useState(false);

  return (
    <Button disabled={loading}>
      {loading ? <RefreshCw className="h-5 w-5 animate-spin" /> : 'Enviar'}
    </Button>
  );
}
```
